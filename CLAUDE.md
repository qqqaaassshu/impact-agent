# CLAUDE.md

Claude Code working in this repository must use `AGENTS.md` as the single source of truth.

Do not duplicate project rules here. If architecture rules, indexing rules, model/privacy rules, commands, or testing requirements change, update `AGENTS.md` first.

## Required Reading

Before making changes, read:

- `AGENTS.md`
- `README.md`

## Local Note

This file only exists because Claude Code looks for `CLAUDE.md`. The effective instructions are intentionally centralized in `AGENTS.md` so Codex, Claude Code, and other agents stay consistent.
