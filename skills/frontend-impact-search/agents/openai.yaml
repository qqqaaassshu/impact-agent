interface:
  display_name: "Frontend Impact Search"
  short_description: "Deterministically search Vue/React repos for field rename evidence"
  default_prompt: "Use $frontend-impact-search to scan a local frontend repository for field rename impact evidence, then return normalized candidates for the Agent to classify."
